[ $INIT ] && exit

eval "$(ssh-agent)"

ssh-add ~/.ami.pem

echo "Getting backups..."
rsync -aPv $BACKUP_SRC ~/

echo "scheduling backups every"
crontab < "0 */2 * * * rsync -aP ~/ $BACKUP_SRC"

echo 'export INIT=true' | tee -a ~/.zshrc
